import { Flex } from "@chakra-ui/react";

const Options = () => {
    return (
        <Flex
        justifyContent='center'
        >
            options
        </Flex>
    )
}

export default Options;